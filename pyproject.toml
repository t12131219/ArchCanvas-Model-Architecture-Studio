[build-system]
requires = ["setuptools>=65.5"]
build-backend = "setuptools.build_meta"

[project]
name = "archcanvas"
version = "0.1.0"
description = "Source-grounded model architecture analysis and review tooling."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.20,<5",
  "libcst>=1.4,<2",
  "pydantic>=2.7,<3",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9", "ruff>=0.8,<1"]
runtime = ["torch>=2.2"]

[project.scripts]
archcanvas = "archcanvas_engine.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["archcanvas_core*", "archcanvas_engine*", "archcanvas_patterns*", "archcanvas_publication*", "archcanvas_python*", "archcanvas_runtime*", "archcanvas_studio*", "archcanvas_transactions*"]

[tool.setuptools.package-data]
archcanvas_core = ["schemas/*.json"]
archcanvas_studio = ["static/*.html", "static/assets/*"]
archcanvas_patterns = ["builtin/*/pattern.json"]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
