# ArchCanvas Studio 更新说明

## Release 标题

**ArchCanvas Studio 0.4.0 — Project Discovery, Safe Source Editing and Reliable Analysis Jobs**

> 对比范围：当前提交 `13e5a32`（对当前项目进行较大程度的优化） vs 上一次提交 `d91f9f4`（对当前项目进行较大程度的优化）

## 更新概览

本次更新将 ArchCanvas Studio 从“可编辑的架构画布”推进为完整的项目分析工作台。重点补齐了项目打开与发现、静态分析任务管理、源码编辑事务、草稿图操作和跨平台开发环境，使用户可以从选择项目开始，经过分析、修改、验证，直到安全提交和重新分析，完成一条闭环工作流。

本次提交共修改 **56 个文件**，新增约 **9,032 行**、删除约 **1,040 行**。

## 主要更新

### 1. 项目打开和文件发现能力升级

Studio 新增项目启动流程。用户可以浏览并选择项目目录，查看扫描到的 Python 文件、配置文件、模型入口和组件层级，再选择具体入口、框架、任务、配置文件和 Conda 环境后启动分析。

项目发现不再只依赖文件名约定，而是通过静态索引解析：

- Python 模块路径和 package `__init__.py`；
- 相对导入、绝对导入、别名和 re-export；
- 类、函数和嵌套组件之间的实例化关系；
- 顶层模型入口与可展开的子组件；
- `torch`、`tensorflow`、`keras`、`jax`、`flax` 等框架证据。

因此，入口可以来自普通 Python callable、类、包导出符号或组件组合，不再要求固定的 `module:Class` 文件布局。项目路径、入口和配置路径均会被限制在项目根目录内，避免扫描或分析越界文件。

### 2. 通用 Python 静态适配器和自动框架识别

本次新增 `python` 通用适配器，并改进 `auto` 框架识别。系统会沿入口模块的导入图收集框架证据，在无法确认具体框架时仍保留 Python 静态数据流，而不是强行套用 PyTorch、Keras 或 JAX 语义。

通用适配器的能力边界是明确的：未知调用和动态控制流会保留为 opaque 边界，系统不会声称拥有未经证明的框架运行时语义。

静态分析器同时增强了：

- 配置字段和嵌套属性表达式解析；
- 模块构造参数来源追踪；
- 深层注册模块和内联调用发现；
- 可达节点过滤，减少与实际输出无关的噪声；
- JSON、TOML、YAML 配置读取。

整个流程仍然只解析源码和配置，不执行用户模型代码。

### 3. 异步分析任务、取消和过期任务保护

Studio 服务端新增统一的 Analysis Job 生命周期：`queued`、`running`、`cancelling`、`succeeded`、`failed`、`cancelled`。

用户可以在界面中查看分析、验证和重新分析任务，取消正在运行的任务，并在 Jobs 面板查看结果。每个项目会分配递增的 generation；切换项目或重新打开项目时，旧 generation 的任务会被取消或标记为过期，旧任务结果不会覆盖当前项目状态。

源码事务提交后，Studio 会自动创建重新分析任务，完成后刷新架构、文件树、画布和 Inspector，避免用户继续查看旧快照。

### 4. 源码工作区和安全写回机制

Studio 新增受快照保护的源码工作区，可直接打开、编辑、保存和丢弃 Python/JSON 缓冲区。编辑过程具备明确的安全约束：

- 文件必须属于当前冻结的源码快照；
- 保存时校验 workspace revision 和文件 base hash；
- 拒绝符号链接、越界路径、非 UTF-8 内容和 NUL 字节；
- 单文件编辑大小限制为 1 MB；
- 活跃事务期间锁定源码缓冲区，避免并发覆盖。

点击“准备验证”后，系统会在隔离工作区中复制项目，生成统一 diff，并重新执行静态分析，计算完整的 Graph Delta。事务只有在解析、静态分析、发布编译和图变化验证全部通过后，才允许进入提交流程。提交采用原子替换；失败时保留原文件并支持丢弃事务。

### 5. 草稿图和高风险修改的可审查性

本次完善了 Draft Graph 操作，包括删除草稿节点、删除连接、预览删除影响、丢弃删除意图，以及草稿连接的端口方向校验。

涉及源码结构的修改不会直接写回文件，而是先记录为带有来源、端口、意图和证明状态的 proposal。对于没有 canonical lowering 的草稿端点，系统会明确标记为 review-required 或 blocked，不会把未经证明的结构伪装成可提交的源码变更。

### 6. 布局候选预览和画布稳定性优化

在上一次拓扑布局和边路由升级的基础上，本次新增布局候选预览：Studio 可以生成 force-directed、radial、orthogonal 等候选布局，先运行几何验证，再由用户选择应用。预览过程不会修改当前文档；只有合格候选才会生成可撤销的 PatchBatch。

同时优化了复合容器的紧凑布局：

- 从叶节点向上收紧容器边界，减少无效留白；
- 处理嵌套容器和同级节点的碰撞；
- 保留流程方向和容器标题空间；
- 重新计算纸张边界；
- 为多条并行边分配稳定端口，降低连线重叠和反向穿越。

这些调整只改变 VisualScene 几何，不改变 Exact IR、节点/边身份和源码证据。

### 7. 交互模块化和前端测试

Studio 前端将高风险交互拆分为可测试模块：

- `drag`：节点拖拽与边界约束；
- `selection`：框选和选区内节点判断；
- `tree`：文件树和导航树展开逻辑；
- `project-launch`：项目入口与模型身份；
- `jobs`：任务轮询、取消和状态转换。

主界面新增项目启动对话框、环境选择、任务面板、源码工作区、布局候选预览、草稿操作和提交后重新分析反馈。文件树、画布、Inspector 和 Jobs 面板共享同一份项目 generation 与 source digest，从交互层阻止旧数据混入当前项目。

### 8. 依赖锁定和 CI 完整验证

本次补充了 Python 3.11 声明、跨平台直接依赖约束和 Linux CPython 3.11 的 PEP 751 lock 文件，并将 PyYAML 纳入正式依赖。

CI 现在会执行：

- `pip check` 依赖一致性检查；
- Ruff 静态检查；
- Python pytest；
- Python 字节码编译检查；
- Schema 导出一致性检查；
- Studio `npm test`；
- Studio TypeScript/Vite 构建。

前端新增 Vitest 测试，覆盖拖拽、任务轮询、项目启动、选择和文件树逻辑。CI 同时固定 Python、Node 和关键包版本，降低不同平台上的结果漂移。

## 升级影响

本次更新扩展了项目会话、源码事务、VisualScene、适配器能力和发布支持矩阵等接口契约。外部集成方如果保存或读取以下对象，需要同步更新 Schema：

- `ProjectSession`：新增环境路径、Python 可执行文件和 generation 信息；
- `SourceTransaction`：新增自由源码编辑、完整 Graph Delta 和验证状态；
- `VisualScene`：新增更丰富的布局和路由元数据；
- `FrameworkAdapterCapability`：新增通用 Python 适配器及形式级能力状态。

已有架构 ID、源码证据和核心语义保持稳定。源码编辑仍遵循“先准备、再验证、后提交”的事务边界。

## 验证状态

- `python -m compileall -q src tools tests`：通过
- `git diff --check`：通过
- 前端 `npm test`：当前工作区未安装 `studio/node_modules`，尚未执行
- 完整 `pytest`：本机默认环境仍使用 Pydantic 1；项目要求 Pydantic 2，因此需要先按 `.github/constraints-py311.txt` 安装依赖

## 相关提交

- `13e5a32`：项目发现、通用静态适配、异步任务、源码工作区、事务提交、布局候选和 CI 完整验证
- `d91f9f4`：拓扑感知布局、边路由编辑、视觉关系渲染和 Studio 交互升级
