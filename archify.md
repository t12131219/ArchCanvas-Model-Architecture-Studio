# ArchCanvas Studio 更新说明

## Release 标题

**ArchCanvas Studio 0.3.0 — Topology-Aware Layout and Interactive Canvas Upgrade**

> 对比范围：当前提交 `d91f9f4`（对当前项目进行较大程度的优化） vs 上一次提交 `7fd5646`（添加了视觉效果和文件树的优化）

## 更新概览

本次更新将 ArchCanvas Studio 从“具备视觉效果的架构查看器”进一步升级为面向模型架构分析的可编辑工作台。更新覆盖了架构图编译、视觉关系推导、自动布局、边路由、画布编辑、文件树联动和几何验证等完整链路。

本次提交共修改 **32 个文件**，新增约 **6,532 行**、删除约 **621 行**。核心目标是：让复杂模型图更容易阅读、更容易调整，同时确保所有视觉编辑都不会破坏源码分析结果和架构语义。

## 主要更新

### 1. 从模型配置驱动改为拓扑和证据驱动的视觉关系

出版编译器不再依赖模型名称或 `architecture_profile` 直接决定图形效果，而是根据规范化架构图中的真实拓扑和证据推导视觉关系，包括：

- 顺序连接（sequence）；
- 并行分支与汇合（parallel branch / merge）；
- 残差连接（residual）；
- 张量形状变换（shape transform）；
- 内存引用、条件控制、路由、状态更新；
- 参数共享与仅训练阶段使用的连接。

这些关系由同一份 VisualScene 数据同时驱动 Studio 画布和 SVG、HTML、PNG、PDF 导出，因此交互界面与导出结果保持一致。画布现在可以准确表达：

- 分叉、汇合和残差外部回路；
- Tensor、Projection、Activation、Normalization、Attention、Condition、Repeat、State、I/O、Opaque 等不同节点形状；
- 与具体连接绑定的张量形状和语义标签；
- 与源码层级一致的容器边界。

系统不会根据参考图片或模型名称虚构节点、边、重复结构或张量形状。所有可见结构都必须来自源码分析、配置、运行时证据或 Exact IR。

### 2. 拓扑感知的发布层级和概览结构

发布编译器现在根据容器树和生产者/消费者关系推导概览层级，将输入准备、归一化/嵌入、处理主干、分解或状态分支、输出后处理组织为更清晰的第一层结构；原始模块仍保留在下层，可继续展开查看。

这消除了过去对固定模型画像的依赖。即使重命名架构或修改画像元数据，只要底层图拓扑不变，系统推导出的布局族和根层级结构也保持稳定，更适合处理未知模型和跨框架模型。

### 3. 新增八种确定性布局模式

Studio 新增可持久化的布局选择器：

- Auto（自动）
- Dual swimlane（双泳道）
- Single lane（单泳道）
- Hierarchical DAG（层级 DAG）
- Branch tree（分支树）
- Force directed（力导向）
- Radial（径向）
- Orthogonal（正交）

`Auto` 是默认模式，根据图拓扑选择保守、可读的布局。其他模式属于展示层调整，只改变 VisualScene 的坐标和连线路径，不改变 Exact IR、PublicationView 身份、溯源绑定以及规范化节点/边 ID。

布局算法保持确定性：力导向布局使用固定种子和固定迭代次数，径向布局使用稳定的图层级和角度顺序，正交布局使用对齐的 rank 行。嵌套容器保持整体结构，跨容器连线通过稳定的边界端口重新路由。

### 4. 自动布局和边路由更加可靠

自动布局现在恢复编译器生成的基线布局，而不是重新拆散并排列每个子节点。这样可以保持嵌套模块的整体性；当子节点被固定（pin）时，其父级单元也不会在恢复布局时被间接移动。

画布支持可交互的边路由编辑：用户可以拖动边的起点或终点，并实时预览候选正交路径。路径评分综合考虑：

- 节点和无关容器碰撞；
- 路径长度与折点数量；
- 交叉和重叠线段；
- 起点/终点端口方向；
- Opaque 节点周围的安全间距。

路由策略包括 `avoid`、`balanced` 和 `compact`。路由调整以视觉补丁的形式保存，因此只改变显示层，不会篡改架构图本身。

### 5. 可编辑画布和交互体验升级

React Studio 新增和完善了以下交互：

- 多节点选择与整体拖拽；
- 批量对齐、分布和移动；
- 节点或顶层单元固定/取消固定；
- 选中内容聚焦，以及文件树与画布之间的双向聚焦；
- 边端点拖动和实时路径预览；
- 左、右、上、下面板的拖拽调整大小；
- 支持键盘操作的面板缩放与恢复默认尺寸；
- 布局模式、面板尺寸、主题和界面语言持久化；
- 中英文界面切换，同时保留必要的英文边标签；
- 移动端 Inspector 展示和 reduced-motion 兼容。

文件树和画布共享同一份投影状态：展开文件树节点会更新当前画布视图，点击画布节点会同步定位到文件树和 Inspector。视觉编辑状态与架构语义保持分离，用户可以调整展示方式，而不会直接改写源码分析结果。

### 6. 视觉规范、渲染器和导出结果优化

VisualScene 现在包含布局族、视觉边关系、丰富的节点形状和显式的边标签位置。SVG 渲染增加了关系元数据、分叉/汇合标记、语义样式和碰撞感知的标签放置。

同一份场景数据可以一致地导出为 SVG、PNG、PDF 和 HTML，确保在线 Studio 与离线发布物的节点、连线、标签和层级表现一致。本次提交同时重建了 Studio 静态资源，使提交到仓库的前端产物与 TypeScript/CSS 源码保持同步。

### 7. Schema 与接口契约同步更新

核心模型和 JSON Schema 扩展了以下契约：

- PublicationView 与 VisualScene；
- VisualSpec 与 VisualRelation；
- 布局模式和路由提示；
- 发布层级元数据；
- Studio 文档状态。

`schemas/` 和 `src/archcanvas_core/schemas/` 下的 Schema 副本同步更新，保证运行时校验、导出文件和公开接口契约一致。

### 8. 测试和验收范围扩大

新增或强化了拓扑布局、全部内置布局模式、确定性坐标、边路由几何、视觉关系渲染、发布层级压缩以及 Studio 交互测试。

验收记录还覆盖了布局切换、Fit/Focus、主题和语言切换、文件夹浏览、路由响应、自动布局恢复、文件树/画布同步，以及折叠和展开视图的几何验证。

## 升级影响

这次更新主要增强展示和交互能力，同时提高了几何验证和溯源约束。现有架构 ID、源码证据和核心语义保持稳定。

如果外部工具会反序列化 `VisualScene`、`PublicationView` 或 Studio view state，建议同步更新 Schema，特别是处理视觉边关系或持久化 `layout_mode` 的调用方。

## 验证状态

- `python -m compileall -q src tools tests`：通过
- `git diff --check`：通过
- `pytest`：当前环境安装的是 Pydantic 1，而项目使用 Pydantic 2 的 `TypeAdapter`、`model_serializer`、`model_validator` 等 API，导致测试尚未进入收集阶段

## 相关提交

- `d91f9f4`：拓扑感知布局、边路由编辑、视觉关系渲染和 Studio 交互升级
- `7fd5646`：视觉效果、文件树导航、层级编辑和跨框架架构支持
